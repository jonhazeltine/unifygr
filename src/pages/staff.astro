---
// Blockified: this page's content lives in content/pages/staff.json and is
// edited in the Page Builder (/studio/builder).
export const prerender = false;
import MountedPage from "../components/builder/MountedPage.astro";
---

<MountedPage slug="staff" />
