---
// Blockified: this page's content lives in content/pages/spiritual-formation.json
// and is edited in the Page Builder (/studio/builder).
export const prerender = false;
import MountedPage from "../components/builder/MountedPage.astro";
import { draftGuard } from "../lib/studio/pages";
import { isAuthed } from "../lib/studio/auth";

// A draft is staff-only, and answers a real 404 to everyone else.
const notFound = await draftGuard("spiritual-formation", isAuthed(Astro.cookies));
if (notFound) return notFound;
---

<MountedPage slug="spiritual-formation" />
